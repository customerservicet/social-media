import { View, TextInput, Text } from "react-native";

export default function Post(props) {
    return (
        <View style={{
            height: 200,
            backgroundColor: "#fff"
        }}></View>
    );
}